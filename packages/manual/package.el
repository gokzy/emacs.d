(require 'cl)

(setq load-path (append
                 '(
		   "~/.emacs.d/packages/manual/js2-mode/"
		   "~/.emacs.d/packages/manual/scala-mode/"
		   "~/.emacs.d/packages/manual/ruby/"
		   )
                 load-path))
